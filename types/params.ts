export interface SectorParams {
  id: string;
  name: string;
}

export interface BaiaParams {
  id: string;
  numeroBaia: string;
  idSector: string;
}

export interface AnimalParams {
  id: string;
}
